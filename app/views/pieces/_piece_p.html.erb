<p>
	<% @pieces.each do |piece|  %>
		<p><%= link_to "#{piece.title} by #{piece.composer}", instrument_piece_path(@instrument, piece), class: 'link-inline' %> - <%= link_to 'Click here to add this piece', new_instrument_piece_mastery_track_path(@instrument, piece), class: 'link-inline' %></p>
	<% end %>
</p>